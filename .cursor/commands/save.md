---
description: Run npm run save — commit, push, always refresh docs
---

When the user asks to **save** the project:

1. Prefer: `npm run save -- "<conventional message>"`
2. Update `architecture.md` / `guardrails.md` prose first if behaviour changed.
3. See also the `git-save` command doc (same script).

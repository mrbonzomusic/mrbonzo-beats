---
import "../styles/global.css";
import { beatstarsLinks, socialLinks } from "../config/links";
import en from "../i18n/en";
import el from "../i18n/el";

interface Props {
  title?: string;
}

const { title = "Mr. Bonzo | Official Website" } = Astro.props;
const logoPath = "/images/logo-main.webp";
const socialPreviewImage = "https://mrbonzo-beats.pages.dev/og-image-v2.jpg";
const siteDescription = "The official website of Mr. Bonzo. Music production, high-quality trap beats, and exclusive drum kits.";
const siteURL = "https://mrbonzo-beats.pages.dev/";

const discordInvite = socialLinks.discord || "https://discord.gg/hmpCHd4J";
const pond5Link = "https://www.pond5.com/artist/mrbonzobeats";
const tikTokLink = "https://www.tiktok.com/@mrbonzomusic";
const ytBeats = "https://www.youtube.com/@mrbonzobeats";
const ytArtist = "https://www.youtube.com/@mrbonzomusic";
const spotifyLink = socialLinks.spotify;
const translations = { en, el };
---

<!doctype html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    
    <meta name="description" content={siteDescription} />
    <meta property="og:url" content={siteURL} />
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={siteDescription} />
    <meta property="og:image" content={socialPreviewImage} />
    <meta property="og:image:type" content="image/jpeg" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={siteDescription} />
    <meta name="twitter:image" content={socialPreviewImage} />
    
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="manifest" href="/manifest.json" />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link rel="preconnect" href="https://www.beatstars.com" crossorigin />
    <link rel="preconnect" href="https://www.googletagmanager.com" crossorigin />
    <link rel="preconnect" href="https://www.google-analytics.com" crossorigin />
    <link rel="dns-prefetch" href="https://gc.zgo.at" />
    <link rel="preconnect" href="https://gc.zgo.at" crossorigin />

    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet" />

    <!-- CSP via meta: Beatstars player + analytics; prefer stricter headers on CDN (see public/_headers) -->
    <meta
      http-equiv="Content-Security-Policy"
      content="default-src 'self'; base-uri 'self'; form-action 'self' https://forms.gle https://docs.google.com; object-src 'none'; upgrade-insecure-requests; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://www.googletagmanager.com https://www.google-analytics.com https://ssl.google-analytics.com https://gc.zgo.at https://www.beatstars.com https://*.beatstars.com https://*.beatstars.net; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; img-src 'self' data: blob: https://*.beatstars.com https://*.beatstars.net https://*.ytimg.com https://*.ggpht.com https://*.googleusercontent.com https://*.scdn.co https://*.spotifycdn.com https://mrbonzo-beats.pages.dev; font-src 'self' https://fonts.gstatic.com data:; frame-src 'self' https://www.beatstars.com https://*.beatstars.com https://*.beatstars.net https://www.youtube.com https://www.youtube-nocookie.com; connect-src 'self' https://www.google-analytics.com https://*.google-analytics.com https://region1.google-analytics.com https://analytics.google.com https://www.googletagmanager.com https://gc.zgo.at https://*.zgo.at https://mrbonzo.goatcounter.com https://*.doubleclick.net https://*.beatstars.com https://*.beatstars.net;"
    />
    <meta http-equiv="X-Content-Type-Options" content="nosniff" />
    <meta name="referrer" content="strict-origin-when-cross-origin" />

    <script async src="https://www.googletagmanager.com/gtag/js?id=G-QFJSZGBGZE"></script>
    <script is:inline>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-QFJSZGBGZE');
    </script>
    <script data-goatcounter="https://mrbonzo.goatcounter.com/count" async src="https://gc.zgo.at/count.js" crossorigin="anonymous"></script>

    <style is:global>
      /* --- BaseBox Banner (in-flow; scrolls away) --- */
      .basebox-banner {
        background: linear-gradient(90deg, #000, #1a001a, #000);
        color: #fff;
        text-align: center;
        padding: 6px 12px;
        font-size: 11px;
        font-weight: 700;
        letter-spacing: 0.02em;
        text-transform: uppercase;
        border-bottom: 1px solid rgba(217, 70, 239, 0.2);
        position: static;
        width: 100%;
        z-index: 30;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 38px;
      }
      
      .basebox-banner a { 
        display: flex; 
        align-items: center; 
        justify-content: center; 
        gap: 6px; 
        color: inherit; 
        text-decoration: none; 
        flex-wrap: wrap;
      }
      .basebox-banner strong { color: #d946ef; font-weight: 900; }
      
      .basebox-badge {
        background: rgba(217, 70, 239, 0.15);
        color: #d946ef;
        padding: 1px 8px;
        border-radius: 99px;
        font-size: 9px;
        border: 1px solid rgba(217, 70, 239, 0.4);
        white-space: nowrap;
        margin-left: 4px;
      }

      @media (max-width: 640px) {
        .basebox-banner { 
          font-size: 8.5px;
          padding: 4px 8px; 
          line-height: 1.2;
        }
        .basebox-badge { font-size: 7.5px; padding: 0px 6px; }
      }

      /* --- Header: sticky under banner --- */
      header#main-header {
        position: sticky !important;
        top: 0 !important;
        left: 0 !important;
        right: 0 !important;
        z-index: 9999999 !important;
        padding-top: 0.75rem;
        padding-bottom: 0.25rem;
        background: linear-gradient(to bottom, rgba(0,0,0,0.92) 0%, rgba(0,0,0,0.75) 70%, transparent 100%);
      }

      body { background-color: black; }

      .nav-divider { width: 1px; height: 14px; background-color: rgba(217, 70, 239, 0.3); margin: 0 6px; flex-shrink: 0; }
      
      .nav-dropdown { position: relative; display: inline-block; padding-bottom: 20px; margin-bottom: -20px; }
      .nav-dropdown-content {
        display: none; position: absolute; background-color: rgba(0, 0, 0, 0.95);
        min-width: 160px; box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.5);
        z-index: 1000; border: 1px solid rgba(217, 70, 239, 0.2);
        border-radius: 14px; margin-top: 5px; backdrop-filter: blur(12px);
      }
      .nav-dropdown:hover .nav-dropdown-content { display: block; animation: dropdownFade 0.2s ease-out; }
      
      @keyframes dropdownFade { from { opacity: 0; transform: translateY(5px); } to { opacity: 1; transform: translateY(0); } }

      #mobile-menu.hidden { display: none; }
      .mobile-link { display: block; padding: 0.85rem 1rem; color: #e5e7eb; font-weight: 800; text-transform: uppercase; font-size: 11px; }
      .mobile-section-label { font-size: 9px; font-weight: 900; text-transform: uppercase; color: #d946ef; padding: 1.5rem 1rem 0.5rem 1rem; opacity: 0.8; }
      @media (max-width: 1023px) {
        .mobile-link { font-size: 10px; padding: 0.65rem 0.85rem; letter-spacing: 0.04em; }
        .mobile-section-label { font-size: 8px; padding: 1.1rem 0.85rem 0.35rem 0.85rem; }
      }
    </style>
  </head>

  <body class="min-h-screen antialiased bg-black text-white">
    <div class="grain-overlay" aria-hidden="true"></div>
    <div id="cursor-glow" class="cursor-glow" aria-hidden="true"></div>

    <div class="basebox-banner">
      <a href="https://forms.gle/AC86bRqwhfNmbCUh9" target="_blank" rel="noopener noreferrer">
        <span>🚀 <strong>BASEBOX</strong>:</span>
        <span data-i18n="basebox.bannerSubtitle">The Automated Artist & Catalog Manager. Send to PROs & Labels in seconds.</span>
        <span class="basebox-badge" data-i18n="basebox.waitlistBtn">GET EARLY ACCESS</span>
      </a>
    </div>

    <header id="main-header">
      <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
        <div class="rounded-full border border-fuchsia-500/20 bg-black/90 backdrop-blur-md px-5 py-2.5 shadow-2xl">
          <div class="flex items-center justify-between">
            <a href="/" class="flex-shrink-0 transition hover:scale-105">
              <img src={logoPath} alt="Logo" data-i18n-alt="ui.logoAlt" width="120" height="30" loading="eager" fetchpriority="high" decoding="async" class="h-[22px] w-auto sm:h-[30px]" />
            </a>

            <nav class="hidden lg:flex items-center gap-x-3 xl:gap-x-4">
              <a href="#player" class="text-[11px] font-bold uppercase tracking-wider text-fuchsia-400 hover:text-fuchsia-300 transition" data-i18n="nav.player">Player</a>
              <a href="#beats" class="text-[11px] font-bold uppercase tracking-wider text-fuchsia-100/70 hover:text-fuchsia-400 transition" data-i18n="nav.beats">Beats</a>
              <a href="#drumkits" class="text-[11px] font-bold uppercase tracking-wider text-fuchsia-100/70 hover:text-fuchsia-400 transition" data-i18n="nav.kits">Kits</a>
              <a href="#latest-releases" class="text-[11px] font-bold uppercase tracking-wider text-fuchsia-100/70 hover:text-fuchsia-400 transition" data-i18n="nav.releases">Releases</a>
              
              <div class="nav-divider"></div>

              <div class="nav-dropdown" tabindex="0">
                <button class="text-[11px] font-bold uppercase tracking-wider text-fuchsia-100/70 hover:text-fuchsia-400 transition flex items-center gap-1.5 outline-none">
                  <span data-i18n="nav.youtube">YouTube</span> <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M19 9l-7 7-7-7"></path></svg>
                </button>
                <div class="nav-dropdown-content">
                  <a href={ytBeats} target="_blank" class="mobile-link" data-i18n="nav.youtubeBeats">Beats Channel</a>
                  <a href={ytArtist} target="_blank" class="mobile-link" data-i18n="nav.youtubeArtist">Artist Channel</a>
                </div>
              </div>

              <a href={spotifyLink} target="_blank" class="text-[11px] font-bold uppercase tracking-wider text-fuchsia-100/70 hover:text-fuchsia-400 transition" data-i18n="nav.spotify">Spotify</a>
              <a href={tikTokLink} target="_blank" class="text-[11px] font-bold uppercase tracking-wider text-fuchsia-100/70 hover:text-fuchsia-400 transition" data-i18n="nav.tiktok">TikTok</a>
              <a href={pond5Link} target="_blank" class="text-[11px] font-bold uppercase tracking-wider text-fuchsia-100/70 hover:text-fuchsia-400 transition" data-i18n="nav.pond5">Pond5</a>
              <a href={discordInvite} target="_blank" class="text-[11px] font-bold uppercase tracking-wider text-fuchsia-100/70 hover:text-fuchsia-400 transition" data-i18n="nav.discord">Discord</a>
              
              <div class="nav-divider"></div>

              <a href="#about" class="text-[11px] font-bold uppercase tracking-wider text-fuchsia-100/70 hover:text-fuchsia-400 transition" data-i18n="nav.about">About</a>
              <a href="#contact" class="text-[11px] font-bold uppercase tracking-wider text-fuchsia-100/70 hover:text-fuchsia-400 transition" data-i18n="nav.contact">Contact</a>
            </nav>

            <div class="flex items-center gap-2">
                <button id="share-btn" type="button" class="p-1.5 text-fuchsia-400 hover:text-fuchsia-300 transition-all active:scale-90" data-i18n-title="ui.shareTitle">
                  <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M8.684 13.342C8.886 12.938 9 12.482 9 12c0-.482-.114-.938-.316-1.342m0 2.684a3 3 0 110-2.684m0 2.684l6.632 3.316m-6.632-6l6.632-3.316m0 0a3 3 0 105.367-2.684 3 3 0 00-5.367 2.684zm0 9.316a3 3 0 105.368 2.684 3 3 0 00-5.368-2.684z" />
                  </svg>
                </button>

                <div id="language-switcher" class="flex items-center gap-1 font-bold text-[10px] border-r border-fuchsia-500/20 pr-3">
                  <button type="button" data-lang-switch="en" class="lang-switch-btn text-fuchsia-200/40 data-[active=true]:text-fuchsia-400 px-1">EN</button>
                  <button type="button" data-lang-switch="el" class="lang-switch-btn text-fuchsia-200/40 data-[active=true]:text-fuchsia-400 px-1">EL</button>
                </div>
                <a href={beatstarsLinks.profile} target="_blank" class="rounded-full bg-fuchsia-600 px-4 py-1.5 text-[10px] font-black uppercase text-white hover:bg-fuchsia-500 transition" data-i18n="nav.store">Store</a>
                <button id="burger-btn" type="button" class="lg:hidden p-1 text-fuchsia-400" data-i18n-aria-label="ui.menuToggleAriaLabel">
                  <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5"><path d="M4 6h16M4 12h16m-7 6h7"/></svg>
                </button>
            </div>
          </div>
        </div>

        <div id="mobile-menu" class="hidden lg:hidden mt-2">
          <div class="rounded-3xl border border-fuchsia-500/20 bg-black/95 backdrop-blur-xl p-2 shadow-2xl max-h-[75vh] overflow-y-auto">
            <div class="mobile-section-label" data-i18n="nav.mobileNavigation">Navigation</div>
            <a href="#player" class="mobile-link text-fuchsia-400" data-i18n="nav.player">Player</a>
            <a href="#beats" class="mobile-link" data-i18n="nav.beats">Beats</a>
            <a href="#drumkits" class="mobile-link" data-i18n="nav.kits">Kits</a>
            <a href="#latest-releases" class="mobile-link" data-i18n="nav.releases">Releases</a>
            <a href="#about" class="mobile-link opacity-80" data-i18n="nav.about">About</a>
            <a href="#contact" class="mobile-link opacity-80" data-i18n="nav.contact">Contact</a>
            <div class="mobile-section-label" data-i18n="nav.mobileSocials">Socials</div>
            <a href={ytBeats} target="_blank" rel="noopener noreferrer" class="mobile-link" data-i18n="nav.youtubeBeats">YouTube Beats</a>
            <a href={ytArtist} target="_blank" rel="noopener noreferrer" class="mobile-link" data-i18n="nav.youtubeArtist">YouTube Artist</a>
            <a href={spotifyLink} target="_blank" rel="noopener noreferrer" class="mobile-link" data-i18n="nav.spotify">Spotify</a>
            <a href={tikTokLink} target="_blank" rel="noopener noreferrer" class="mobile-link" data-i18n="nav.tiktok">TikTok</a>
            <a href={pond5Link} target="_blank" rel="noopener noreferrer" class="mobile-link" data-i18n="nav.pond5">Pond5</a>
            <a href={discordInvite} target="_blank" rel="noopener noreferrer" class="mobile-link" data-i18n="nav.discord">Discord</a>
          </div>
        </div>
      </div>
    </header>

    <main class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <slot />
    </main>

    <script is:inline define:vars={{ translations }}>
      function resolveString(dict, path) {
        if (!dict || !path || typeof path !== 'string') return null;
        var parts = path.split('.');
        var cur = dict;
        for (var i = 0; i < parts.length; i++) {
          if (cur == null || typeof cur !== 'object' || !(parts[i] in cur)) return null;
          cur = cur[parts[i]];
        }
        return typeof cur === 'string' ? cur : null;
      }

      function normalizeLang() {
        try {
          var stored = localStorage.getItem('selected-lang');
          if (stored === 'en' || stored === 'el') return stored;
          if (stored) localStorage.removeItem('selected-lang');
        } catch (e) {}
        return 'en';
      }

      function applyTranslations() {
        var lang = normalizeLang();
        var dict = translations[lang] || translations.en;
        document.documentElement.lang = lang;

        var htmlKeys = { 'drumkits.title': true, 'about.titleHtml': true, 'contact.title': true };

        function safeAttr(val) {
          return String(val).replace(/[\u0000-\u001F<>]/g, '');
        }

        document.querySelectorAll('[data-i18n]').forEach(function (node) {
          var key = node.getAttribute('data-i18n');
          var val = resolveString(dict, key) || resolveString(translations.en, key);
          if (!val) return;
          if (htmlKeys[key]) node.innerHTML = val;
          else node.textContent = val;
        });
        document.querySelectorAll('[data-i18n-alt]').forEach(function (node) {
          var key = node.getAttribute('data-i18n-alt');
          var val = resolveString(dict, key) || resolveString(translations.en, key);
          if (val) node.setAttribute('alt', safeAttr(val));
        });
        document.querySelectorAll('[data-i18n-title]').forEach(function (node) {
          var key = node.getAttribute('data-i18n-title');
          var val = resolveString(dict, key) || resolveString(translations.en, key);
          if (val) node.setAttribute('title', safeAttr(val));
        });
        document.querySelectorAll('[data-i18n-aria-label]').forEach(function (node) {
          var key = node.getAttribute('data-i18n-aria-label');
          var val = resolveString(dict, key) || resolveString(translations.en, key);
          if (val) node.setAttribute('aria-label', safeAttr(val));
        });
        document.querySelectorAll('.lang-switch-btn').forEach(function (btn) {
          btn.setAttribute('data-active', btn.getAttribute('data-lang-switch') === lang ? 'true' : 'false');
        });
      }

      function adjustLayout() {
        var header = document.getElementById('main-header');
        if (header) {
          header.style.top = '';
        }
        document.body.style.paddingTop = '';
      }

      // ΑΜΕΣΗ ΕΚΤΕΛΕΣΗ πριν το load για αποφυγή του "τρεμουλιάσματος"
      applyTranslations();
      adjustLayout();

      function initEventListeners() {
        document.querySelectorAll('.lang-switch-btn').forEach(function (btn) {
          btn.onclick = function () {
            var code = btn.getAttribute('data-lang-switch');
            if (code === 'en' || code === 'el') localStorage.setItem('selected-lang', code);
            applyTranslations();
            setTimeout(adjustLayout, 30);
          };
        });

        const bBtn = document.getElementById('burger-btn');
        const mMenu = document.getElementById('mobile-menu');
        if (bBtn) {
          bBtn.onclick = (e) => { 
            e.stopPropagation(); 
            mMenu.classList.toggle('hidden'); 
          };
        }

        const shareBtn = document.getElementById('share-btn');
        if (shareBtn) {
          shareBtn.onclick = async function () {
            try {
              var lang = normalizeLang();
              var dict = translations[lang] || translations.en;
              var shareTitle = resolveString(dict, 'ui.shareNativeTitle') || 'Mr. Bonzo Official';
              if (navigator.share) await navigator.share({ title: shareTitle, url: window.location.href });
              else {
                await navigator.clipboard.writeText(window.location.href);
                var msg = resolveString(dict, 'ui.linkCopied') || 'Link copied!';
                alert(msg);
              }
            } catch (err) {}
          };
        }
      }

      // Event listeners για αλλαγή μεγέθους και φόρτωμα
      window.addEventListener('resize', adjustLayout);
      window.addEventListener('load', () => {
        adjustLayout();
        initEventListeners();
      });

      // Υποστήριξη για Astro View Transitions (αν τα χρησιμοποιείς)
      document.addEventListener('astro:page-load', () => {
        applyTranslations();
        adjustLayout();
        initEventListeners();
      });
    </script>
    <script is:inline>
      (function () {
        function initSectionReveal() {
          if (window.__sectionRevealIo) {
            try {
              window.__sectionRevealIo.disconnect();
            } catch (e) {}
            window.__sectionRevealIo = null;
          }
          if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
            document.querySelectorAll('.section-reveal').forEach(function (el) {
              el.classList.add('is-revealed');
            });
            return;
          }
          var nodes = document.querySelectorAll('.section-reveal:not(.is-revealed)');
          if (!nodes.length) return;
          if (!('IntersectionObserver' in window)) {
            nodes.forEach(function (el) {
              el.classList.add('is-revealed');
            });
            return;
          }
          var io = new IntersectionObserver(
            function (entries) {
              entries.forEach(function (en) {
                if (en.isIntersecting) {
                  en.target.classList.add('is-revealed');
                  io.unobserve(en.target);
                }
              });
            },
            { root: null, rootMargin: '0px 0px -6% 0px', threshold: 0.08 }
          );
          window.__sectionRevealIo = io;
          nodes.forEach(function (el) {
            io.observe(el);
          });
        }

        function initCursorGlow() {
          if (window.__mrbonzoCursorFx) return;
          var glow = document.getElementById('cursor-glow');
          if (!glow) return;
          window.__mrbonzoCursorFx = true;
          var mq = window.matchMedia('(pointer: fine) and (min-width: 1024px)');
          var tx = -100;
          var ty = -100;
          var x = -100;
          var y = -100;
          var vx = 0;
          var vy = 0;
          var rafId = 0;

          function pointOverBeatstarsIframe(px, py) {
            var list = document.querySelectorAll('.beatstars-embed-iframe');
            for (var i = 0; i < list.length; i++) {
              var el = list[i];
              var r = el.getBoundingClientRect();
              if (px >= r.left && px <= r.right && py >= r.top && py <= r.bottom) return true;
            }
            return false;
          }

          function tick() {
            rafId = 0;
            if (!glow.classList.contains('is-active')) return;
            var tension = 0.48;
            var friction = 0.58;
            vx = (vx + (tx - x) * tension) * friction;
            vy = (vy + (ty - y) * tension) * friction;
            x += vx;
            y += vy;
            var dist = Math.hypot(tx - x, ty - y);
            if (dist < 1.2) {
              x = tx;
              y = ty;
              vx = 0;
              vy = 0;
            }
            var under = null;
            try {
              under = document.elementFromPoint(tx, ty);
            } catch (e) {}
            var overIframe = pointOverBeatstarsIframe(tx, ty);
            var hover = !!(
              overIframe ||
              (under &&
                (under.tagName === 'IFRAME' ||
                  (under.closest &&
                    under.closest(
                      'a, button, [role="button"], input[type="submit"], input[type="button"], label, .lang-switch-btn, #burger-btn, #share-btn'
                    ))))
            );
            glow.classList.toggle('is-hover', hover);
            glow.style.transform = 'translate3d(' + x + 'px,' + y + 'px,0) translate(-50%,-50%)';
            rafId = requestAnimationFrame(tick);
          }

          function onMove(e) {
            tx = e.clientX;
            ty = e.clientY;
            if (!glow.classList.contains('is-active')) {
              glow.classList.add('is-active');
              x = tx;
              y = ty;
              vx = 0;
              vy = 0;
            }
            if (!rafId) rafId = requestAnimationFrame(tick);
          }

          function onLeave() {
            glow.classList.remove('is-active', 'is-hover');
            if (rafId) {
              cancelAnimationFrame(rafId);
              rafId = 0;
            }
          }

          function applyMq() {
            onLeave();
            document.body.classList.remove('has-cursor-fx');
            document.removeEventListener('mousemove', onMove);
            document.removeEventListener('mouseleave', onLeave);
            if (!mq.matches) return;
            document.body.classList.add('has-cursor-fx');
            document.addEventListener('mousemove', onMove, { passive: true });
            document.addEventListener('mouseleave', onLeave);
          }

          if (typeof mq.addEventListener === 'function') {
            mq.addEventListener('change', applyMq);
          } else if (mq.addListener) {
            mq.addListener(applyMq);
          }
          applyMq();
        }

        initSectionReveal();
        initCursorGlow();
        document.addEventListener('astro:page-load', initSectionReveal);
      })();
    </script>
  </body>
</html>